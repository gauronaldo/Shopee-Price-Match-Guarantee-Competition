# Synthetic test fixtures

`smoke/` is a deliberately tiny, generated-by-hand dataset that mirrors the expected Shopee
metadata columns. Its six PPM images are abstract color blocks and contain no competition or
private content. The three label groups exist only to exercise contracts and deterministic smoke
checks; they must never be used to report model quality.
