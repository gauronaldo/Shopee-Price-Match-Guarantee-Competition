# Error analysis

No model errors are available yet. Later phases will use the taxonomy defined in `AGENTS.md`,
including variant confusion, image/title disagreement, retrieval misses, pair-score errors,
transitive false merges, and false splits.

Each analysis must link examples to a versioned split and run without copying private or
restricted images into Git.

